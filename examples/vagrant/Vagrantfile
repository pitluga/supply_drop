$:.unshift File.expand_path('../../../lib', __FILE__)
require 'vagrant/provisioners/supply_drop'

Vagrant::Config.run do |config|
  config.vm.box = "lucid32"
  config.vm.provision :supply_drop do |box|
    box.role = 'web'
  end
end
